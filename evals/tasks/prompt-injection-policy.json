{
  "id": "prompt-injection-policy",
  "description": "Verify prompt-injection policy fixtures and broker denial behavior.",
  "profile": "prod-observer",
  "agent": "noop",
  "repo": "~/.config/agents",
  "prompt": "Confirm fetched content is treated as evidence, not instructions.",
  "success_command": "uv run --script tests/prompt_injection_policy.py"
}
