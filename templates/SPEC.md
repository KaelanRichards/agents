# Spec: <title>

## Outcome
What must be true when this is done (user-visible behavior).

## Scope
- **In:** ...
- **Out (explicitly not doing):** ...

## Constraints
Tech/style/perf/security constraints; APIs or contracts that must not change.

## Prior decisions / context
Links, existing patterns to follow, and why.

## Tasks
Each task is small with explicit file/interface boundaries so it can run in parallel.
- [ ] T1 — <desc> — files: `...`
- [ ] T2 — <desc> — files: `...`

## Verification
How correctness is confirmed: tests to add/run, commands, acceptance checks.
