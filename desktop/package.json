{
	"name": "agents-desktop",
	"version": "0.1.0",
	"private": true,
	"description": "Desktop control center for the agents environment (Tauri 2)",
	"scripts": {
		"tauri": "tauri",
		"dev": "tauri dev",
		"build": "tauri build",
		"icons": "uv run --python 3.12 scripts/make_icons.py"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2"
	}
}
