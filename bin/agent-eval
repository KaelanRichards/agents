#!/usr/bin/env bash
# agent-eval — run small local eval tasks against agent profiles.
set -euo pipefail
AH="${AGENTS_HOME:-$HOME/.config/agents}"
exec python3 "$AH/scripts/agent_control.py" eval "$@"
