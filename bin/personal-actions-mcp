#!/usr/bin/env bash
# personal-actions-mcp — narrow Slack/Gmail/Calendar write facade for Claude + Codex.
# The server loads its chmod-600 secrets env file itself (no interactive dependency at startup).
exec uv run --quiet --script "$HOME/.config/agents/mcp-servers/personal-actions/server.py" "$@"
