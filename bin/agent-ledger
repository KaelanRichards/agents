#!/usr/bin/env bash
# agent-ledger — append-only local ledger for agent runs and events.
set -euo pipefail
AH="${AGENTS_HOME:-$HOME/.config/agents}"
exec python3 "$AH/scripts/agent_control.py" ledger "$@"
